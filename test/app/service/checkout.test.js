'use strict'

const { app, assert } = require('egg-mock/bootstrap')

describe('test for checkout service', () => {
  // it('should get checkout info', async () => {
  //   const ctx = app.mockContext()

  //   const checkoutInfo = await ctx.service.checkout.getCheckoutInfo()

  //   assert(checkoutInfo)
  // })

  // it('should submit order without error', async () => {
  //   const ctx = app.mockContext()

  //   const orderSubmited = await ctx.service.checkout.submitOrder(1)
  //   // assert(orderSubmited)
  // })
})
