'use strict'

const { app, assert } = require('egg-mock/bootstrap')

describe('test for cart service', () => {
  // it('should get all cart info', async () => {
  //   const ctx = app.mockContext()

  //   const cartInfo = await ctx.service.cart.getCart()

  //   assert(cartInfo)
  // })

  // it('should get one goods which goods_id = 1', async () => {
  //   const ctx = app.mockContext()

  //   const goods = await ctx.service.product.findById(1)
  //   assert(goods)
  // })
})
